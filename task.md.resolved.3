# Checklist: Módulo de Autenticação/Segurança

- [x] Modificar CSS Base (`index.css`)
  - [x] Adicionar classes `.login-wrapper` para fundo radial 100vh overlay.
  - [x] Adicionar classes de estilos premium para Inputs, Labels e Botões principais do formulário.
- [x] Atualizar componente Dashboard (`App.jsx`)
  - [x] Habilitar o `useState` de `isAuthenticated` (default `false`).
  - [x] Estruturar o retorno condicional do Login (trava o DOM se deslogado).
  - [x] Construir a UI local de Login interativa e de alto design (SaaS).
  - [x] Ancorar evento de mudança booleana (`setIsAuthenticated(false)`) ao Botão `Sair` do menu retrátil.
